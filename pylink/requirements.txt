pyautogui>=0.9.54
pynput>=1.7.7

# Eye control (optional: for gaze + blink input)
# Install for eye and blink control; PixelLink runs without these if unavailable.
opencv-python>=4.8.0
mediapipe>=0.10.0

# Voice module dependencies
# TTS: ElevenLabs Flash v2.5
elevenlabs>=1.0.0
python-dotenv>=1.0.0

# Note: pydub has issues with Python 3.13 (audioop removed)
# On macOS, we use afplay directly; on other systems, install pydub:
# pydub>=0.25.1
# For Python 3.13+: pip install pyaudioop

# STT: Whisper ONNX (faster-whisper)
faster-whisper>=1.0.0
pyaudio>=0.2.14

# LLM Brain: OpenAI
openai>=1.0.0

# Browser automation with AI (browser-use)
# Install: pip install browser-use && uvx browser-use install
browser-use>=0.11.0
# Optional: langchain-openai for OpenAI fallback (if not using BROWSER_USE_API_KEY)
langchain-openai>=0.3.0

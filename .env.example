# Maes / Electron + MCP environment configuration
# Copy this file to `.env` (or export vars in your shell) and restart Maes.

# --- Runtime toggles (mostly used by the Electron bridge) ---
# MAES_DRY_RUN=1
# MAES_SPEED=1.0
# MAES_ENABLE_KILL_SWITCH=0

# --- Google Calendar MCP (optional) ---
# To enable `calendar-mcp`, create a Google OAuth "Desktop" client and download
# the client secrets JSON. Then set these paths.
#
# NOTE: If you leave these unset, calendar tools will not register (no errors).
#
# MAES_CALENDAR_CREDENTIALS_PATH=/absolute/path/to/google-calendar-client-secret.json
# MAES_CALENDAR_TOKEN_PATH=/absolute/path/to/maes-calendar-token.json

# --- Gmail MCP (optional) ---
# Same idea as Calendar: OAuth Desktop client secrets JSON + a token cache path.
#
# MAES_GMAIL_CREDENTIALS_PATH=/absolute/path/to/gmail-client-secret.json
# MAES_GMAIL_TOKEN_PATH=/absolute/path/to/maes-gmail-token.json

# --- Notes/Reminders MCP (macOS only) ---
# No API keys needed. macOS will prompt for Automation permissions the first time.

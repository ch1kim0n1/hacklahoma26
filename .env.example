# PixelLink / Electron + MCP environment configuration
# Copy this file to `.env` (or export vars in your shell) and restart PixelLink.

# --- Runtime toggles (mostly used by the Electron bridge) ---
# PIXELINK_DRY_RUN=1
# PIXELINK_SPEED=1.0
# PIXELINK_ENABLE_KILL_SWITCH=0

# --- Google Calendar MCP (optional) ---
# To enable `calendar-mcp`, create a Google OAuth "Desktop" client and download
# the client secrets JSON. Then set these paths.
#
# NOTE: If you leave these unset, calendar tools will not register (no errors).
#
# PIXELINK_CALENDAR_CREDENTIALS_PATH=/absolute/path/to/google-calendar-client-secret.json
# PIXELINK_CALENDAR_TOKEN_PATH=/absolute/path/to/pixelink-calendar-token.json

# --- Gmail MCP (optional) ---
# Same idea as Calendar: OAuth Desktop client secrets JSON + a token cache path.
#
# PIXELINK_GMAIL_CREDENTIALS_PATH=/absolute/path/to/gmail-client-secret.json
# PIXELINK_GMAIL_TOKEN_PATH=/absolute/path/to/pixelink-gmail-token.json

# --- Notes/Reminders MCP (macOS only) ---
# No API keys needed. macOS will prompt for Automation permissions the first time.
